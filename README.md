# git-sandbox
A Repository for learning and practicing git

git clone
git log
git log --oneline
git add .
git status
git commit -am "Add Avijit's file"
git-sandbox$ git config --global user.name "Avijit Ghosh"

git push